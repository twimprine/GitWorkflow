# PRP Orchestrator Configuration Template
# Copy this to .env and fill in your values

# ============================================================================
# REQUIRED: Anthropic API Key
# ============================================================================
# Get your API key from: https://console.anthropic.com/settings/keys
# Each environment (prod/qa/dev) should have its own key for better tracking
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ============================================================================
# OPTIONAL: Environment Identifier
# ============================================================================
# Used for logging and tracking purposes
# Values: dev, qa, prod
ENVIRONMENT=dev

# ============================================================================
# Rate Limiting (Control Costs)
# ============================================================================
# Maximum number of batch API calls per hour
# Recommended: prod=1, qa=5, dev=10
MAX_BATCHES_PER_HOUR=1

# Minimum minutes to wait between batch submissions
# Recommended: prod=60, qa=20, dev=10
MIN_BATCH_INTERVAL_MINUTES=60

# ============================================================================
# Polling Intervals
# ============================================================================
# How often to check queue for new definitions (seconds)
QUEUE_CHECK_INTERVAL_SECONDS=300

# How often to poll Anthropic API for batch status (seconds)
BATCH_POLL_INTERVAL_SECONDS=90

# ============================================================================
# Timeouts
# ============================================================================
# Maximum hours to wait for a batch to complete
# Anthropic batches typically complete within minutes to hours
BATCH_TIMEOUT_HOURS=24

# ============================================================================
# Optional: Project Settings
# ============================================================================
# PROJECT_NAME=MyProject  # Used in logs for identification

# ============================================================================
# Example Configurations by Environment
# ============================================================================

# Development (fast iteration, higher costs)
# ENVIRONMENT=dev
# ANTHROPIC_API_KEY=sk-ant-dev-...
# MAX_BATCHES_PER_HOUR=10
# MIN_BATCH_INTERVAL_MINUTES=0

# QA (moderate pace, controlled costs)
# ENVIRONMENT=qa
# ANTHROPIC_API_KEY=sk-ant-qa-...
# MAX_BATCHES_PER_HOUR=5
# MIN_BATCH_INTERVAL_MINUTES=20

# Production (careful, cost-controlled)
# ENVIRONMENT=prod
# ANTHROPIC_API_KEY=sk-ant-prod-...
# MAX_BATCHES_PER_HOUR=1
# MIN_BATCH_INTERVAL_MINUTES=60
