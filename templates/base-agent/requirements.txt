# Core dependencies
# Python 3.8+ built-in modules (asyncio, typing) not needed

# Testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
pytest-benchmark>=4.0.0

# Development dependencies
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
pre-commit>=3.0.0

# Monitoring and logging
# structlog>=23.0.0  # Optional - using standard logging instead
# prometheus-client>=0.16.0  # Optional - for metrics